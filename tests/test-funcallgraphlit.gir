graph foo(graph g) {
	node n;
	n.set_data(6);
	g.add_node(n);
	return g;
}

graph bar(graph g1, graph g2) {
	for_node(n : g2) {
		g1.add_node(n);
	}
	return g1;
}

int main() {
	!~ graph g = foo([E:7]); ~!
	for_node(n : bar(foo([A:5 -- B:3]), [C:7 -- D:8])) {
		print(n.data());
	}

	return 0;
}