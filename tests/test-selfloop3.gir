int main() {
	graph<int> g = [A:3 -- A -- B:4 -- C:5];
	bfs(n : g ; A) {
		print(n.data());
	}
	dfs(n : g ; A) {
		print(n.data());
	}

	digraph<int> g2 = [A <-> A -> B <- C];
	bfs(n : g2 ; A) {
		print(n.data());
	}
	dfs(n : g2 ; A) {
		print(n.data());
	}

	return 0;
}